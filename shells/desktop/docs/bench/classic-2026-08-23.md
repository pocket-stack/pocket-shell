# Pocket Desktop classic baseline (2026-08-23)

**Measured source:** Pocket Desktop `65947066e602fe4dc1bdec45e1d168dd76ca8d25`, PocketJS `dc49eeed6c6693aaffb12d832d7a5f4cebbcc715`
**Machine:** Apple M3 Max, 128 GiB, macOS 26.5.2 (25F84)

| Metric | Result |
|---|---:|
| Native host executable | 8.75 MiB |
| System UI JS + PAK | 1.01 MiB |
| 11 installed app JS + PAK artifacts | 15.58 MiB |
| Complete installed runtime | 25.35 MiB |
| Cold start to first painted frame, median of 10 | 179.62 ms |
| Settled idle RSS, median of 10 | 86.73 MiB |
| Settled physical footprint | 294.24 MiB |
| Maximum idle process-tree count | 1 |

## Protocol

The release build contains the classic System UI and all 11
installed Pocket applications. Size values are logical file bytes. The complete
installed runtime is the native host, every resolved package's JS and PAK, and
the resolved System plan; build intermediates, source files and debug symbols
are excluded.

Cold start is measured from process spawn to the host's first-painted-frame
`READY` marker. One warmup launch is excluded, so these are **process-cold,
cache-warm** launches rather than disk-cold boots. The recorded samples are:
170.59, 168.97, 180.19, 180.12, 172.79, 177.43, 188.17, 181.15, 179.12, 193.05 ms.

For memory, the System reaches its first frame, remains hands-off for
20 seconds, and is then sampled 10 times at
2-second intervals. RSS comes from `ps` over the
whole process tree. Physical footprint comes from macOS `footprint` after
the RSS samples. **The process-tree count pins the one-native-process System
architecture; AppInstances are isolated JavaScript realms, not OS child
processes.** Raw samples and per-file sizes are in
`classic-2026-08-23.json`.

Reproduce on an unlocked macOS desktop:

```sh
bun run desktop build
bun run desktop benchmark:classic
```
